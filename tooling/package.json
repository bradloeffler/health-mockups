{
  "name": "health-mockups-tooling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FORGE mock-check + gallery-gen harness for health-mockups",
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "check": "node mock-check.mjs",
    "gallery": "node gen-gallery.mjs"
  },
  "devDependencies": {
    "glob": "^10.5.0",
    "playwright": "^1.47.0"
  }
}
